# AI Memory – Vehicle Detection Demo (Web)

This file tracks enhancements, decisions, and tech debt for the project.

---

## Enhancement history

### 2026-05-09 – Đồng bộ pipeline Camera 2 với Camera 1 (counting/congestion/model info)

**Yêu cầu:**
- Camera 2 cũng phải chạy đầy đủ pipeline như camera 1 (counting line, congestion, model info, traffic-light feed) thay vì chỉ detect + publish frame.

**Đã thay đổi:**
- `backend/app/services/stream_service.py`:
  - Thêm cụm state riêng cho camera 2: tracker/counter/congestion/counting_line để không tranh chấp state camera 1.
  - Trong `_companion_worker()`, bổ sung xử lý parity với camera 1:
    - dựng `tracks` từ detections
    - tính line theo ROI companion (hoặc line mặc định)
    - chạy `VehicleCounter.update(...)`
    - cập nhật congestion companion
    - lấy model info hiện hành cho payload companion
    - giữ feed traffic-light lane observation cho companion.
  - Đưa `lane_stats` vào header companion WebSocket (không đổi contract polling hiện tại).
  - Đồng bộ reset/update tracker giữa primary và companion khi reset counters / đổi tracker type.

**Verification:**
- `python -m py_compile backend/app/services/stream_service.py` -> pass.
- `ReadLints` file sửa -> không có lỗi mới.

### 2026-05-09 – Fix lệch box so với xe ở H264 overlay

**Symptom:**
- Ở chế độ `H264`, box detect đôi khi bị lệch khỏi phương tiện (thường bị “đẩy đi trước”).

**Root cause:**
- `frontend/src/components/VideoPlayer/VideoPlayer.tsx` có cơ chế motion compensation cho `overlayOnly` (H264 path), tự cộng thêm độ dịch theo vận tốc track.
- Với FPS thấp/jitter hoặc nhịp detection không đều, độ dịch có thể overshoot và làm box lệch hình học so với xe thực tế.

**Fix applied:**
- Tắt motion compensation mặc định bằng cờ `ENABLE_H264_MOTION_COMPENSATION = false` để ưu tiên bám đúng tọa độ backend.

**Verification:**
- `ReadLints` file `frontend/src/components/VideoPlayer/VideoPlayer.tsx`: không có lỗi mới.
- Kỳ vọng: box bám đúng vị trí xe hơn trong mode H264, giảm hiện tượng lệch rõ rệt.

### 2026-05-09 – Giảm fallback giả khi H264 bị jitter/packet lỗi ngắn

**Symptom:**
- Đang chạy H264 lâu lâu log có lỗi decode/RTSP (`hevc ...`, `retrieveFrame Picture does not contain data`) và UI tự nhảy về JPEG.

**Root cause:**
- Frontend `H264LivePlayer` fallback quá nhạy: chỉ cần một số lỗi ngắn (`player error`/`play reject`) là gọi fallback ngay.
- Dashboard đang dùng `setH264Mode(false)` khi bất kỳ camera nào lỗi, kéo toàn bộ màn hình về JPEG.

**Fix applied:**
- `frontend/src/components/VideoPlayer/H264LivePlayer.tsx`:
  - Thêm bộ đếm error burst theo cửa sổ thời gian; chỉ fallback khi lỗi dồn dập + không có frame tiến triển đủ lâu.
  - Watchdog dựa trên idle time (không cập nhật frame trong thời gian dài) thay vì điều kiện quá gắt.
- `frontend/src/pages/Dashboard/Dashboard.tsx`:
  - Đổi fallback từ global sang theo từng slot camera (`primary/companion/extra2/extra3`).
  - Camera nào lỗi thì camera đó rơi về JPEG, các camera còn lại vẫn giữ H264.

**Verification:**
- `npm run build` frontend: pass.
- Lint các file sửa: không có lỗi mới.

### 2026-05-09 – H264 mode áp dụng cho tất cả camera LIVE

**Yêu cầu:**
- Khi bật nút `H264/JPEG`, tất cả camera đang stream (camera 1/2/3/4) đều dùng H264, không chỉ camera chính.

**Root cause:**
- Kiến trúc cũ chỉ có 1 FFmpeg relay H264 + 1 WS endpoint `/ws/stream-h264` cho camera chính.
- Camera companion/extra chỉ có JPEG path nên toggle H264 không thể áp dụng đồng loạt.

**Fix applied:**
- Backend:
  - `backend/app/api/ws_routes.py`:
    - Thêm manager H264 theo slot (`primary`, `companion`, `extra2`, `extra3`).
    - Thêm route `GET /ws/stream-h264/{slot}`.
  - `backend/app/services/ffmpeg_relay_service.py`:
    - Relay service nhận `slot` và broadcast về manager tương ứng.
    - Tạo 4 relay instances: primary/companion/extra2/extra3.
  - `backend/app/services/stream_service.py`:
    - Start/stop relay H264 tương ứng khi start/stop companion và extra streams.
  - `backend/app/api/stream_routes.py`:
    - Mở rộng `GET /api/v1/stream/h264/status?slot=...` để xem trạng thái relay theo từng camera.
- Frontend:
  - `frontend/src/components/VideoPlayer/H264LivePlayer.tsx`:
    - Thêm prop `wsPath` để chọn kênh H264 theo camera.
  - `frontend/src/pages/Dashboard/Dashboard.tsx`:
    - Camera 2/3/4 dùng `H264LivePlayer` khi `h264Mode=true`, overlay box vẫn lấy từ luồng detect tương ứng.

**Verification:**
- `python -m py_compile` các file backend đã sửa: pass.
- `npm run build` frontend: pass.
- ReadLints frontend files đã sửa: không lỗi mới.

### 2026-05-09 – Giảm trễ box khi xem chế độ H264

**Symptom:**
- Khi bật chế độ `H264`, bounding box detect hiển thị chậm và có cảm giác "đuổi theo sau xe".

**Root cause:**
- Luồng hiển thị `H264` (MPEG-TS relay) có độ trễ thấp hơn luồng detect overlay.
- Trong lúc đó pipeline detect có thể đang bật `skip_frames > 0`, làm detections cập nhật thưa hơn và gây lệch pha rõ rệt khi xe di chuyển nhanh.

**Fix applied:**
- `frontend/src/pages/Dashboard/Dashboard.tsx`:
  - Thêm cơ chế tự động ép `skip_frames=0` khi `h264Mode=true` và stream đang chạy.
  - Lưu giá trị `skip_frames` cũ vào `h264SkipBackupRef` để khôi phục khi tắt H264 hoặc dừng stream.
  - Nhờ vậy giữ được nhịp detections sát thời gian thực hơn trong H264 mode.

**Verification:**
- Lint file đã sửa: không có lỗi mới.
- Kỳ vọng: ở chế độ H264, box bám sát xe hơn, giảm hiện tượng box đi sau.

### 2026-05-09 – Auto recover TensorRT static imgsz mismatch (320 vs 416)

**Symptom:**
- Detect/tracking “mất hẳn”, log backend spam:
  - `Pipeline error (skipping frame): input size torch.Size([1, 3, 320, 320]) not equal to max model size (1, 3, 416, 416)`.

**Root cause:**
- Model đang chạy TensorRT `.engine` static shape `416x416`, nhưng runtime infer truyền `imgsz=320` từ config.
- Mỗi frame bị exception trong `yolo_model.track()` nên stream worker bỏ qua detections.

**Fix applied:**
- `backend/app/ml/yolo_model.py`:
  - Thêm parse lỗi TensorRT shape mismatch.
  - Khi gặp mismatch, tự động lưu override `imgsz` theo `max model size` của engine và retry infer ngay frame đó.
  - Áp dụng cho cả `track()` và `predict()`.
  - Reset override khi unload model.

**Expected result:**
- Không còn spam lỗi `320 != 416`.
- Detect/tracking tự hoạt động lại dù `.env` đang để `YOLO_IMGSZ` không khớp engine static.

### 2026-05-09 – Fix mất detect/tracking khi payload thiếu frame

**Symptom:**
- UI báo stream vẫn chạy nhưng detect/tracking thỉnh thoảng “mất hẳn” hoặc không cập nhật ổn định.

**Evidence:**
- `GET /api/v1/stream/frame` có lúc trả `detections` hợp lệ nhưng `frame=null` (tình huống thường gặp khi frontend đang ưu tiên H264 và luồng JPEG bị gián đoạn ngắn).
- `frontend/src/hooks/useDetection.ts` đang `return` sớm khi payload WS không có `frame/frame_blob`, nên bỏ qua luôn `detections` + `stats`.
- Polling fallback cũng yêu cầu bắt buộc `payload.frame`, nếu thiếu frame thì không cập nhật detect.

**Root cause:**
- Hook frontend ghép chặt logic detect vào sự hiện diện của frame, thay vì xử lý detect/stats độc lập.

**Fix applied:**
- `frontend/src/hooks/useDetection.ts`:
  - Bỏ early-return theo điều kiện `frame`.
  - Luôn xử lý `detections` + `stats` khi payload hợp lệ.
  - Chỉ cập nhật `currentFrame` khi thực sự có frame mới.
  - Polling fallback không bỏ qua payload chỉ vì thiếu frame trong một nhịp.

**Verification:**
- Lint file sửa: không có lỗi mới.
- Kỳ vọng sau fix: detect/tracking không bị “đơ” chỉ vì một vài payload thiếu `frame`.

### 2026-05-09 – Root cause H264 relay không chạy (bytes_sent=0)

**Symptom:**
- Stream chính vẫn `active` nhưng H264 relay báo `running=true` và `bytes_sent=0`, UI đen khi ở mode H264.

**Evidence:**
- Process ffmpeg relay đang chạy đúng command line.
- Chạy ffmpeg thủ công với cùng nguồn RTSP cho thấy luồng HEVC phát sinh rất nhiều lỗi decode (`PPS id out of range`, `Skipping invalid undecodable NALU`...), nghĩa là stderr phát sinh liên tục.
- Trong code cũ `ffmpeg_relay_service`, process được mở với `stderr=PIPE` nhưng không có luồng đọc stderr.

**Root cause:**
- Pipe `stderr` bị đầy (backpressure), ffmpeg bị kẹt I/O và không tiếp tục đẩy MPEG-TS ra `stdout`.
- Worker relay đọc `stdout` nên thấy rỗng, dẫn đến `bytes_sent` không tăng.

**Fix applied:**
- `backend/app/services/ffmpeg_relay_service.py`:
  - Thêm thread `_drain_stderr()` đọc stderr liên tục để tránh deadlock.
  - Lưu tail lỗi gần nhất vào `_stderr_tail` và cập nhật `_last_error` để chẩn đoán.
  - Quản lý lifecycle thread stderr khi start/stop relay.

**Verification guidance:**
- Restart backend, connect lại camera, kiểm tra:
  - `GET /api/v1/stream/h264/status` -> `bytes_sent` tăng dần, `throughput_kbps > 0`.
  - UI H264 hiển thị hình, không còn đen kéo dài.

### 2026-05-09 – Fix màn hình đen khi bật H264 mode

**Symptom:**
- Khung camera chính đen toàn màn hình khi đang ở mode `H264`, dù stream chính vẫn báo `active` và có FPS.

**Evidence:**
- Runtime check:
  - `GET /api/v1/stream/status` trả `active=true`, `fps>0`.
  - `GET /api/v1/stream/h264/status` trả `running=true` nhưng `bytes_sent=0`, `throughput_kbps=0.0`.
- Điều này cho thấy pipeline chính còn chạy, nhưng luồng H264 không có frame hiển thị tới player tại thời điểm lỗi.

**Root cause (UI-level impact):**
- `H264LivePlayer` chỉ fallback về JPEG khi bắt được sự kiện `error`.
- Trường hợp relay H264 không ra frame (không đủ để trigger error rõ ràng), UI vẫn giữ mode H264 nên màn hình đen kéo dài.

**Fix applied (small/safe):**
- `frontend/src/components/VideoPlayer/H264LivePlayer.tsx`:
  - Thêm startup timeout 5s: nếu không thấy frame đầu tiên (`loadeddata/playing/timeupdate`) thì tự gọi fallback.
  - Thêm watchdog định kỳ để phát hiện trạng thái “không phát được frame” và fallback.
  - Cleanup đầy đủ timer/event listener khi unmount.

**Verification:**
- Lint file sửa: không có lỗi mới.
- Kỳ vọng hành vi sau fix: nếu H264 không phát được thì UI tự chuyển sang JPEG path thay vì treo màn hình đen.

**Remaining risk:**
- Cần kiểm tra thêm backend relay (FFmpeg/NVENC/NVDEC) để xử lý triệt để nguyên nhân gốc khiến `bytes_sent` có thể bằng 0.

### 2026-05-09 – Phase 1/2/3: FFmpeg H264 relay + frontend low-CPU mode + status/fallback

**Mục tiêu:** Giảm tải CPU bằng luồng live H264 qua FFmpeg NVENC/NVDEC, giữ fallback JPEG cũ.

**Đã thay đổi (Phase 1 – backend engine):**
- Thêm `backend/app/services/ffmpeg_relay_service.py`:
  - chạy process FFmpeg relay RTSP -> MPEG-TS (H264 NVENC) qua `pipe:1`
  - broadcast chunk bytes ra WebSocket manager riêng
  - quản lý start/stop/status + error
  - fallback tìm `ffmpeg` theo `FFMPEG_BIN`, PATH, hoặc WinGet link path.
- `backend/app/services/stream_service.py`:
  - khi start primary stream: khởi chạy FFmpeg relay song song
  - khi stop stream: dừng relay.

**Đã thay đổi (Phase 2 – transport + frontend player):**
- `backend/app/api/ws_routes.py`:
  - thêm manager `ws_h264_manager`
  - thêm endpoint `GET /ws/stream-h264` cho luồng MPEG-TS bytes.
- `frontend/src/components/VideoPlayer/H264LivePlayer.tsx`:
  - thêm player H264 live qua `mpegts.js` (WS path `/ws/stream-h264`)
  - có callback lỗi để fallback mode.
- `frontend/src/pages/Dashboard/Dashboard.tsx`:
  - tích hợp `H264LivePlayer` cho camera chính khi `h264Mode` bật.
  - hiển thị badge mode `H264/JPEG`.
- `frontend/package.json`:
  - thêm dependency `mpegts.js`.

**Đã thay đổi (Phase 3 – fallback/ops):**
- `backend/app/api/stream_routes.py`:
  - thêm `GET /api/v1/stream/h264/status` trả trạng thái relay (running/throughput/error).
- Frontend fallback:
  - nếu H264 player báo lỗi -> tự tắt `h264Mode` và dùng lại `VideoPlayer` JPEG path.

**Kiểm tra kỹ thuật:**
- Python compile các file backend mới/sửa -> pass.
- Frontend `npm run build` -> pass.
- Lint -> không lỗi mới ở các file đã chạm.

**Ghi chú vận hành:**
- Cần restart backend sau pull/sửa để route mới có hiệu lực.
- FFmpeg đã được cài qua winget trong phiên này; nếu relay báo thiếu ffmpeg, set `FFMPEG_BIN` tới `ffmpeg.exe`.

### 2026-05-09 – Áp preset max-FPS balanced + ép runtime TensorRT

**Yêu cầu:** Tăng FPS thêm nhưng vẫn giữ chất lượng tương đối; tận dụng GPU tối đa hơn.

**Đã thay đổi:**
- `backend/.env`:
  - ép backend infer dùng TensorRT: `YOLO_BACKEND=tensorrt`
  - áp preset max-FPS balanced:
    - `YOLO_IMGSZ=320`
    - `INFERENCE_SKIP_FRAMES=3`
    - `STREAM_MAX_WIDTH=800`
    - `STREAM_JPEG_QUALITY=58`
    - `MAX_FPS=20`
    - `COMPANION_MAX_FPS=20`
- `backend/.env.example`:
  - bổ sung khối gợi ý preset max-FPS balanced tương ứng để dễ áp dụng cho môi trường mới.

**Giải thích kỹ thuật:**
- TensorRT tối ưu phần infer trên GPU.
- Các tham số còn lại chủ yếu giảm bottleneck CPU ở khâu resize/encode/transport, giúp FPS end-to-end tăng rõ hơn so với chỉ tối ưu infer.

### 2026-05-09 – Export TensorRT chạy nền + endpoint trạng thái

**Vấn đề:** `POST /api/v1/models/export-engine` chạy đồng bộ nên request treo lâu và có thể làm người dùng tưởng backend bị đơ.

**Đã thay đổi:**
- `backend/app/api/model_routes.py`:
  - Chuyển `POST /export-engine` sang chạy background thread.
  - Nếu đang có job export khác chạy thì trả `409`.
  - Thêm trạng thái job in-memory (`running/done/ok/error/model/engine/started_at/ended_at`).
  - Thêm `GET /api/v1/models/export-engine/status` để theo dõi tiến độ/kết quả.

**Kỳ vọng:**
- API export trả về ngay, không timeout client.
- Người dùng theo dõi được kết quả export thay vì chờ request treo.

**Kiểm tra sau sửa:**
- `python -m py_compile backend/app/api/model_routes.py` -> pass.
- Lint file sửa -> không lỗi mới.

### 2026-05-09 – Run one-click ưu tiên GPU/TensorRT

**Yêu cầu:** Tích hợp toàn bộ setup GPU/TensorRT vào `run.bat` để chỉ chạy một lần.

**Đã thay đổi:**
- `run.bat`:
  - Đổi cơ chế venv: ưu tiên **root** `venv-gpu` (tránh nhầm với `backend\venv-gpu`).
  - Tự tạo venv bằng `py -3.11` (fallback `python` nếu thiếu launcher).
  - Cài dependencies backend bằng đúng python của venv.
  - Tự cài TensorRT từ NVIDIA index.
  - Tự đảm bảo torch CUDA (`cu126`) bằng index PyTorch.
  - Thêm bước health-check nhanh:
    - in version `torch` + `cuda.is_available()`
    - thử import `tensorrt` và in version.
  - Khởi động backend bằng venv root vừa cấu hình.

**Kỳ vọng tác động:**
- Người dùng chỉ cần chạy `run.bat` là có môi trường backend đúng chuẩn GPU-first (torch CUDA + TensorRT) trước khi start server.
- Giảm lỗi cấu hình tay giữa nhiều venv khác nhau.

**Rủi ro/ghi chú:**
- Lần chạy đầu sẽ rất lâu vì tải gói lớn (torch CUDA + tensorrt).
- Nếu mạng chập chờn, cài TensorRT có thể fail; script hiện cảnh báo và vẫn cho phép chạy backend.

### 2026-05-09 – One-click TensorRT export endpoint

**Yêu cầu:** Làm quy trình chuyển sang TensorRT đơn giản (không phải thao tác tay nhiều bước).

**Đã thay đổi:**
- `backend/app/models/detection_model.py`:
  - Thêm request model `ModelExportEngineRequest` cho API export engine.
- `backend/app/ml/yolo_model.py`:
  - Thêm `export_engine(path, fp16, workspace_gb, imgsz)`:
    - chỉ nhận nguồn `.pt`
    - yêu cầu CUDA
    - gọi Ultralytics export `format=engine` và trả về path `.engine`.
- `backend/app/services/model_service.py`:
  - Thêm `export_engine(...)` để export và tùy chọn load ngay engine.
- `backend/app/api/model_routes.py`:
  - Thêm `POST /api/v1/models/export-engine`:
    - export `.pt -> .engine`
    - `load_after_export=true` mặc định để tự chuyển runtime sang engine.

**Kỳ vọng tác động:**
- Vận hành dễ hơn: chỉ cần gọi một API để tạo và dùng TensorRT engine.
- Giảm rủi ro quên bước “load engine sau khi export”.

**Kiểm tra sau sửa:**
- `python -m py_compile` các file liên quan -> pass.
- Lint -> không có lỗi mới.

### 2026-05-09 – GPU-first runtime: TensorRT preference + profile tune

**Yêu cầu:** Tận dụng GPU tối đa hơn nữa thay vì để CPU quá tải.

**Đã thay đổi:**
- `backend/app/core/config.py`:
  - Thêm biến cấu hình backend runtime:
    - `YOLO_BACKEND` (`auto|torch|tensorrt`)
    - `YOLO_TRT_AUTO_EXPORT`
    - `YOLO_TRT_FP16`
    - `YOLO_TRT_WORKSPACE_GB`
- `backend/app/ml/yolo_model.py`:
  - Thêm chọn backend runtime theo ưu tiên:
    - `auto/tensorrt` + CUDA: ưu tiên load sidecar `.engine` nếu có.
    - Nếu bật `YOLO_TRT_AUTO_EXPORT=true` và đang load `.pt`, thử export TensorRT engine rồi load lại engine.
    - Nếu lỗi/không đủ điều kiện -> fallback an toàn về PyTorch.
  - Thêm `self._runtime_backend` và tách kwargs infer theo backend:
    - TensorRT: gọi infer/track không ép `device/half` kiểu torch.
    - Torch: giữ logic `device` + `half` như cũ.
  - Log rõ model được load bằng backend nào (`torch` hoặc `tensorrt`).
- `backend/app/services/model_service.py` + `backend/app/api/model_routes.py`:
  - Cho phép liệt kê/upload model `.engine` bên cạnh `.pt`.
- `backend/.env` + `backend/.env.example`:
  - Bổ sung biến TensorRT backend.
  - Tune profile giảm CPU để GPU không chờ CPU:
    - `YOLO_IMGSZ=416`
    - `INFERENCE_SKIP_FRAMES=2`
    - `STREAM_MAX_WIDTH=960`
    - `STREAM_JPEG_QUALITY=65`
    - `MAX_FPS=18`
    - `COMPANION_MAX_FPS=18`

**Kỳ vọng tác động:**
- Khi có TensorRT engine, infer chạy backend TensorRT giúp tăng GPU utilization và FPS.
- Khi chưa có TensorRT, vẫn chạy ổn định nhờ fallback torch và profile giảm bottleneck CPU.

**Kiểm tra sau sửa:**
- `python -m py_compile` cho các file backend đã sửa -> pass.
- Lint các file đã sửa -> không có lỗi mới.

**Rủi ro/ghi chú:**
- Auto export TensorRT phụ thuộc môi trường TensorRT/driver/CUDA tương thích; vì vậy mặc định vẫn để `YOLO_TRT_AUTO_EXPORT=false`.
- OpenCV vẫn encode JPEG trên CPU; muốn NVENC end-to-end cần pipeline ffmpeg riêng (không có sẵn trong codebase hiện tại).

### 2026-05-09 – Tăng tận dụng GPU decode + giảm CPU encode dư

**Yêu cầu:** CPU đang quá tải, FPS thấp; cần tận dụng GPU nhiều hơn cho pipeline stream.

**Root cause còn lại:**
- Mở `VideoCapture` RTSP chủ yếu theo đường mặc định nên hardware decode chưa luôn được ưu tiên rõ ràng.
- Worker vẫn `cv2.imencode()` gần như mọi vòng lặp, kể cả lúc không có client HTTP poll hoặc WebSocket nhận frame -> tốn CPU không cần thiết.

**Fix đã áp dụng:**
- `backend/app/services/stream_service.py`:
  - Thêm helper `_open_video_capture(url, is_rtsp=...)`:
    - luôn set `OPENCV_FFMPEG_CAPTURE_OPTIONS` qua `_rtsp_ffmpeg_options()`
    - khi `RTSP_HWACCEL=true`, thử mở capture với `CAP_PROP_HW_ACCELERATION`/`CAP_PROP_HW_DEVICE` (best-effort)
    - nếu không hỗ trợ thì fallback an toàn về `cv2.CAP_FFMPEG` như cũ.
  - Thay toàn bộ điểm mở lại capture (primary/companion/extra/reconnect) sang helper chung để decode path nhất quán.
  - Chỉ encode JPEG khi thực sự có consumer:
    - Primary: encode khi có HTTP poll gần đây hoặc có client WebSocket.
    - Companion/Extra: áp dụng tương tự theo từng kênh.
    - Khi không có consumer, vẫn giữ detect/tracking/stats nhưng bỏ qua `imencode + base64`.

**Kỳ vọng cải thiện:**
- Giảm đáng kể CPU ở phần encode/base64 khi dashboard không mở hoặc chỉ xem một phần kênh.
- Tăng cơ hội decode RTSP bằng phần cứng trên build OpenCV/FFmpeg hỗ trợ, giúp GPU utilization cao hơn và FPS ổn định hơn.

**Kiểm tra sau sửa:**
- `python -m py_compile backend/app/services/stream_service.py` -> pass.
- Lint file sửa đổi -> không có lỗi mới.

**Rủi ro/ghi chú:**
- `CAP_PROP_HW_ACCELERATION` phụ thuộc build OpenCV; code đã fallback tự động nên không làm crash nếu không hỗ trợ.
- Nếu mục tiêu là FPS cao nhất khi đang mở dashboard liên tục, cần tune thêm `.env` theo tải thực tế (ví dụ `YOLO_IMGSZ`, `INFERENCE_SKIP_FRAMES`, `STREAM_MAX_WIDTH`).

### 2026-05-09 – Giảm nghẽn CPU, đẩy decode RTSP sang GPU (CUDA hwaccel)

**Symptom:**
- Chạy 2 camera detect đồng thời: CPU cao (~80%+), GPU compute không tăng tương xứng.

**Evidence:**
- Runtime xác nhận CUDA khả dụng (`torch 2.11.0+cu126`, `torch.cuda.is_available() = True`).
- `nvidia-smi` thấy GPU đang hoạt động nhưng CPU vẫn là nút thắt.
- Code path RTSP decode vẫn chủ yếu do OpenCV/FFmpeg trên CPU.

**Fix đã áp dụng:**
- `backend/app/core/config.py`:
  - Thêm cờ `RTSP_HWACCEL: bool = False`.
- `backend/app/services/stream_service.py`:
  - Thêm helper `_rtsp_ffmpeg_options()` để build FFmpeg options.
  - Khi `RTSP_HWACCEL=true`, tự thêm `hwaccel;cuda|hwaccel_output_format;cuda` cho RTSP capture (primary/companion/extra).
- `backend/app/api/stream_routes.py`:
  - Thumbnail RTSP cũng dùng cùng hwaccel option khi bật.
- `backend/.env`:
  - Đặt `YOLO_DEVICE=cuda` để ép infer dùng GPU.
  - Bật `RTSP_HWACCEL=true` để thử chuyển decode sang GPU.
- `backend/.env.example`:
  - Bổ sung tài liệu cấu hình `RTSP_HWACCEL`.

**Kỳ vọng:**
- Giảm tải decode trên CPU, giúp pipeline ổn định hơn khi 2 camera cùng detect.
- GPU utilization tăng ở engine decode/compute tùy luồng camera và model.

**Rủi ro/rollback:**
- Một số build OpenCV-FFmpeg không hỗ trợ hwaccel CUDA hoàn chỉnh; nếu gặp lỗi mở stream/khung đen, tắt `RTSP_HWACCEL=false` rồi restart backend.

### 2026-05-09 – Tối ưu 2 camera detect đồng đều (không hạ riêng camera phụ)

**Yêu cầu:** Cả 2 camera đều phải detect giống nhau, không giảm chất lượng/logic riêng cho camera 2.

**Root cause hiệu năng:**
- Khi chạy 2 luồng cùng detect, YOLO + encode chạy song song làm CPU/GPU quá tải, FPS hiển thị tụt.
- Camera chính chưa tái sử dụng detections khi bật `INFERENCE_SKIP_FRAMES` (khác camera companion), nên nếu bật skip dễ bị cảm giác “mất box” giữa các frame.

**Fix đã áp dụng:**
- `backend/app/services/stream_service.py`:
  - Ở worker camera chính, thêm `last_dets` và khi frame bị skip thì dùng lại detections gần nhất (giống cơ chế companion) thay vì trả rỗng.
- `backend/.env`:
  - Chuyển sang profile cân bằng cho 2 camera detect đồng đều:
    - `MAX_FPS=15`
    - `COMPANION_MAX_FPS=15`
    - `YOLO_IMGSZ=480`
    - `STREAM_MAX_WIDTH=1280`
    - `STREAM_JPEG_QUALITY=70`
    - `INFERENCE_SKIP_FRAMES=1`
    - `RTSP_FLUSH_FRAMES=1`
- `backend/.env.example`:
  - Đồng bộ comment gợi ý theo profile 2 camera detect.

**Kết quả kỳ vọng:**
- Hai camera giữ cùng logic detect/tracker, giảm tải đồng đều toàn pipeline.
- FPS ổn định hơn khi chạy đồng thời 2 luồng, hạn chế tụt mạnh/nhấp nháy box.

**Rủi ro còn lại:**
- Nếu máy đang chạy CPU hoặc GPU yếu, có thể cần giảm thêm `YOLO_IMGSZ` xuống `416` hoặc tăng `INFERENCE_SKIP_FRAMES=2`.

### 2026-05-09 – Refactor Camera 2 worker theo kiến trúc Camera 1

**Mục tiêu:** Làm camera 2 (companion) chạy ổn định/mượt tương tự camera 1 bằng cùng kiến trúc xử lý realtime.

**Đã thay đổi:**
- `backend/app/services/stream_service.py`:
  - Refactor `_companion_worker()` từ vòng lặp tuần tự sang mô hình 2 tầng giống primary:
    - capture thread riêng
    - queue `maxsize=1` (drop-frame: luôn giữ frame mới nhất)
    - main processing loop đọc frame từ queue
  - Thêm logic reconnect RTSP khi read fail liên tiếp (pattern giống `_worker`).
  - Áp dụng các knob hiệu năng đã dùng ở primary cho companion:
    - resize trước encode theo `STREAM_MAX_WIDTH`
    - inference skipping theo `INFERENCE_SKIP_FRAMES` + tái sử dụng `last_dets`
  - Giữ nguyên contract output companion (`frame`, `detections`, `fps`, `frame_count`, `stream_active`) cho API polling + WebSocket.
  - Bổ sung cleanup rõ ràng ở `finally` (stop event, join capture thread, clear latest, reset running flag).

**Kết quả kỹ thuật:**
- Camera 2 tránh tích lũy trễ khi infer/encode chậm nhờ cơ chế drop-frame.
- Đồng bộ hành vi với camera 1 nên dễ tune hiệu năng theo cùng bộ settings runtime.

**Kiểm tra sau sửa:**
- `python -m py_compile backend/app/services/stream_service.py` → pass.
- Lint file sửa đổi → không có lỗi mới.

**Follow-up khuyến nghị:**
- Tune `.env` thực tế cho 2 camera đồng thời (`COMPANION_MAX_FPS`, `STREAM_MAX_WIDTH`, `INFERENCE_SKIP_FRAMES`) để đạt mức mượt theo phần cứng đang dùng.

### 2026-05-09 – Nghiên cứu tối ưu Camera 2 mượt như Camera 1

**Yêu cầu:** Khi bật thêm camera thứ 2, luồng phải ổn định và mượt tương đương camera 1.

**Phân tích hiện trạng:**
- `primary` (`_worker`) dùng kiến trúc 2 tầng: capture thread + queue size 1 + drop-frame nên luôn ưu tiên khung mới nhất, giảm tích lũy trễ.
- `companion` (`_companion_worker`) đang chạy tuần tự trong 1 vòng lặp (`read -> flush -> infer -> encode -> publish -> sleep`) nên nếu infer/encode chậm sẽ kéo tụt toàn pipeline.
- `COMPANION_MAX_FPS` mặc định chỉ `12` (trong `backend/app/core/config.py`), thấp hơn rõ rệt so với camera chính (`MAX_FPS=30`).
- `companion` chưa áp dụng các knob hiệu năng đã có ở primary (`STREAM_MAX_WIDTH`, `INFERENCE_SKIP_FRAMES`) nên có thể tốn CPU hơn mức cần thiết cho cùng độ mượt hiển thị.
- Khi WebSocket companion không ổn định, frontend fallback polling `/stream/companion/frame` mỗi 250ms có thể tạo cảm giác khựng/nhấp nháy hơn luồng chính.

**Kế hoạch tối ưu đề xuất (phase):**
1. **Phase 1 – Cân bằng cấu hình runtime (nhanh, rủi ro thấp)**
   - Tăng `COMPANION_MAX_FPS` từ `12` lên `15-20` tùy GPU/CPU.
   - Giảm tải per-frame cho camera 2: đặt `STREAM_MAX_WIDTH` (ví dụ 960/1280), `STREAM_JPEG_QUALITY` 65-75.
   - Bật `INFERENCE_SKIP_FRAMES=1` hoặc `2` nếu ưu tiên độ mượt hiển thị hơn mật độ infer.
2. **Phase 2 – Đồng bộ kiến trúc camera 2 với camera 1**
   - Refactor `_companion_worker` sang mô hình capture thread + queue maxsize=1 như `_worker`.
   - Áp dụng cùng chính sách drop-frame + RTSP reconnect để chống drift.
3. **Phase 3 – Quan sát và cảnh báo chất lượng**
   - Bổ sung metric riêng cho companion: capture fps, infer fps, sent fps, avg inference ms.
   - Hiển thị cảnh báo khi companion rơi về polling fallback hoặc FPS tụt dưới ngưỡng.

**Rủi ro & giảm thiểu:**
- Tăng FPS companion có thể làm GPU/CPU spike nếu model nặng -> cần tune cùng `YOLO_IMGSZ` và `INFERENCE_SKIP_FRAMES`.
- Refactor worker dễ phát sinh race condition khi start/stop nhanh -> cần lock/running flags rõ ràng và timeout join.
- Nhiều RTSP cùng nguồn NVR có thể bị giới hạn session/bitrate -> nên test theo từng cặp camera thực tế.

**Recommended first action:**
- Làm Phase 1 trước bằng cấu hình `.env` (không đổi logic), đo lại FPS camera 1/2 trong 10-15 phút, sau đó mới quyết định có cần refactor Phase 2 hay không.

### 2026-05-07 – Simplify Traffic Light UI

**Scope:** Tối giản panel đèn giao thông theo phản hồi “rườm rà”.

**Changes made (frontend):**
- `frontend/src/components/TrafficLight/TrafficLightPanel.tsx`: Chỉ hiển thị **2 cột đèn** (đỏ/vàng/xanh) + **chú thích RTSP/đường** cho Màn 1 & Màn 2 (từ `phaseRoadLabels`). Bỏ các phần chữ/thống kê phụ và các nút điều khiển trong panel.

### 2026-05-06 – Rebuild Dashboard UI (UI v2)

**Scope:** Bỏ UI Dashboard hiện tại và xây lại theo kiểu “bảng điều hành” tập trung vào điểm chính.

**Changes made (frontend):**
- **Dashboard layout:** Viết lại `frontend/src/pages/Dashboard/Dashboard.tsx` thành UI v2: 3 cột (Controls / Video / Situation), KPI header + KPI cards, bảng “detections gần nhất”, event log, Counter + TrafficLight + Congestion.
- **Kept functionality:** Connect/Stop stream, Model upload/load, ROI drawing (overlay), quick settings (conf/line/tracker/mode), export CSV, reset counts.
- **Stability:** Chạy `npm run build` xác nhận compile OK.

**Fix included:**
- `frontend/src/components/VideoPlayer/index.ts`: gỡ export types không tồn tại (`VideoPlayerHandle`, `VideoTransform`) để tránh lỗi build TypeScript.

### 2025-03-14 – Full system alignment with spec (enhance)

**Scope:** Align the existing FastAPI + React codebase with the full professional spec (clean architecture, model storage, UI, docs).

**Changes made:**

1. **Backend**
   - **Model upload storage:** Uploaded `.pt` files are now saved to `models_storage` (MODELS_DIR) instead of `uploads`, matching the “dedicated folder” requirement. `model_routes.py` and `model_service._resolve()` updated; list still includes both directories for backward compatibility.
   - **.env.example:** Added `backend/.env.example` and root `.env.example` for PORT, DEBUG, CORS, detection defaults.

2. **Frontend – UI (light, professional)**
   - **Theme:** Switched from dark (green/cyan) to a **light, minimal theme** per spec: white/gray/blue, no flashy colors.
   - **Tailwind:** `bg-base` → `#f8fafc`, `bg-card` → white, `accent` → `#2563eb`, `accent-2` → `#64748b`; danger/warning muted.
   - **Global:** `index.css` – light background, slate text, blue range thumb, gray scrollbar.
   - **Components:** Dashboard, VideoPlayer, RoiDrawer, CounterPanel, ModelUploader – all updated to slate/white/blue borders and text; toasts and status pills use light backgrounds.
   - **Canvas:** Bounding box and ROI colors in `utils/canvas.ts` and CounterPanel class colors set to blue/gray palette; counting line set to blue.

3. **Documentation**
   - **README:** Rewritten for FastAPI + React + Docker: project objective, tech stack, project structure, quick start (Docker + local), configuration, **ffmpeg RTSP test** command, API table, dashboard layout, detection pipeline, extendability note.

4. **Behavior preserved**
   - All existing API contracts, WebSocket payload, ROI format, and detection pipeline unchanged.
   - No breaking changes to frontend API or hooks.

**Tradeoffs:**
   - Single active model and upload-to-models_storage are now the default; legacy uploads in `uploads/` still load via `_resolve()`.
   - Light theme only; no dark/light toggle (can be added later).

**Remaining / tech debt:**
   - Docker frontend: in production build, frontend is served separately; if needed, configure reverse proxy or `VITE_API_URL` so `/api` reaches the backend.
   - No automated tests run in this session; manual smoke test recommended (upload model, start stream, set ROI, check counts).
   - Tracker is selectable: **SORT** / **DeepSORT** via config `TRACKER_TYPE` and UI dropdown in Cài Đặt. (CentroidTracker removed for project cleanliness.)

---

## Architecture decisions

- **AI in `ml/` only:** YOLO, ROI filter, tracker, counter live under `backend/app/ml/`; routes and services orchestrate only.
- **One active model:** Only one YOLO model loaded at a time; upload replaces file in `models_storage` and loads it.
- **WebSocket for real-time:** Single endpoint `/api/v1/stream/ws` streams base64 frame + detections + stats; frontend draws on canvas.
  (Note: later removed; system now uses REST + /stream/status only, no WebSocket.)
- **ROI:** Polygon as list of `[x, y]`; backend filters detections by centroid-in-polygon when ROI is active.

---

## Performance / baselines

- Backend: FPS capped by `MAX_FPS` (default 30); frame interval applied in stream worker.
- Frontend: No baseline captured; consider measuring WebSocket frame rate and canvas draw time if needed.

---

## Pain points (known)

- ROI coordinates from frontend are canvas-relative; scaling to video coordinates may be needed for different resolutions (currently backend accepts as-is; ROI filter uses integer points).
- No auth on API or WebSocket; suitable for demo/lab only.

---

## Bug fixes

### 2026-03-14 – Backend crash on import after removing CentroidTracker

**Symptom:** Backend không khởi động được, crash ngay khi import module với `ModuleNotFoundError: No module named 'sort_tracker'`.

**Expected:** Backend start bình thường; nếu thiếu tracker dependency thì báo lỗi rõ ràng lúc startup.

**Root cause:** `stream_service` singleton tạo `StreamService()` tại import-time, và trong `__init__` khởi tạo tracker ngay (`get_tracker('sort')`) → import fail trước khi lifespan startup check chạy.

**Fix:** Khởi tạo tracker **lazy** trong `StreamService` (để import không crash). Việc kiểm tra dependency được thực hiện trong `app.main` lifespan (fail-fast với thông báo cài đặt). Đồng thời `_get_sort_class()` raise ImportError với message rõ ràng khi thiếu `sort-tracker-py`.

**Affected files:** `backend/app/services/stream_service.py`, `backend/app/ml/tracker.py`, `backend/app/main.py`.

### 2025-03-14 – Duplicate ROI UI at bottom of screen (layout bug)

**Symptom:** ROI panel content (instructions, Draw/Undo/Clear/Apply buttons, FPS/Frame/ROI badges, 0 điểm, Inactive) appeared twice: once in the left sidebar (correct) and again at the bottom of the main area, causing overflow and a broken layout.

**Expected:** Single ROI controls in sidebar; only a transparent canvas overlay on top of the video for drawing.

**Evidence:** In Dashboard, `RoiOverlay` was implemented as `<RoiDrawer ... />`, so the full RoiDrawer (text + buttons + canvas) was rendered a second time in the main column. That second instance flowed in normal document order below the VideoPlayer, so all its content showed at the bottom.

**Root cause:** RoiOverlay rendered the entire RoiDrawer component instead of only the canvas overlay. Two separate RoiDrawer instances (sidebar + main) also had independent state, so the overlay canvas was not the one tied to the sidebar controls.

**Fix applied:**
- Lifted ROI state (`roiPoints`, `roiDrawing`) to Dashboard.
- Introduced **RoiCanvasOverlay**: canvas-only component, absolutely positioned over the video, receiving shared state from Dashboard.
- RoiDrawer in the sidebar now runs in controlled mode (receives `points`, `setPoints`, `isDrawing`, `setIsDrawing` from Dashboard).
- Replaced `RoiOverlay` (full RoiDrawer) in the main area with `RoiCanvasOverlay` using the same state. Removed the unused `RoiOverlay` wrapper.
- `handleClearRoi` clears local ROI state (`setRoiPoints([])`, `setRoiDrawing(false)`).

**Affected files:** `frontend/src/pages/Dashboard/Dashboard.tsx`, `frontend/src/components/RoiDrawer/RoiDrawer.tsx`, `frontend/src/components/RoiDrawer/index.ts`.

**Lessons:** When the same component is used in two places (sidebar + overlay), avoid rendering the full UI in both; split into “controls” and “canvas” and share state from the parent to prevent duplication and wrong layout. Responsive/layout issue was not due to viewport CSS but to duplicate DOM and flow.

---

### 2025-03-14 – YouTube URL support (enhance)

**Scope:** Allow pasting a YouTube link directly as video source (in addition to RTSP and local file path).

**Changes made:**
- **video_utils.py:** Added `is_youtube_url(url)` (regex for youtube.com/watch, youtu.be, embed) and `resolve_youtube_url(url)` using `yt-dlp -g` via subprocess to get direct stream URL. OpenCV cannot open YouTube URLs directly; yt-dlp resolves to a stream URL.
- **stream_service.py:** Before starting the capture thread, if the input URL is a YouTube link, resolve it and use the resolved URL for `_probe_line_y` and `_worker`. If resolution fails, raise `ValueError` with a clear message (install yt-dlp or check link).
- **requirements.txt:** Added `yt-dlp>=2024.1.0`.
- **Frontend:** Updated placeholder and empty-state copy to mention "link YouTube" / "RTSP, link YouTube, hoặc đường dẫn video".

**Tradeoffs:** Depends on yt-dlp; some YouTube formats may not be openable by OpenCV (e.g. DASH-only). If resolution fails, user sees a clear error. No API change; same `POST /stream/start` with `url` body.

**Remaining:** Not all YouTube videos may work (region, format, age-restricted). Optional: document or add ffmpeg fallback for problematic formats.

---

### 2025-03-14 – Connect bị đứng (request không trả về)

**Symptom:** Bấm Connect trên dashboard thì giao diện “đứng” (loading mãi), không báo lỗi, không vào stream.

**Expected:** POST /stream/start trả về trong vài giây; nếu URL sai thì báo lỗi hoặc stream không có frame.

**Evidence:** Trong `stream_service.start()` có gọi `_probe_line_y(effective_url)`. Hàm này mở `cv2.VideoCapture(url)` và `cap.read()` ngay trên luồng xử lý HTTP. Với RTSP/URL chậm hoặc không tồn tại, OpenCV có thể block rất lâu (hoặc không timeout), nên request POST không bao giờ trả về, frontend kẹt ở `await startStream(...)` (setConnecting(true) mãi).

**Root cause:** Probe stream (mở capture + đọc 1 frame) chạy đồng bộ trong request handler, khiến HTTP response bị block.

**Fix:** Bỏ gọi `_probe_line_y(effective_url)` trong `start()`. Worker thread đã tự set `_counting_line_y` ở frame đầu (khi `_counting_line_y is None`). API trả về ngay; worker mở stream ở background — nếu mở thất bại thì không có frame, `stream_active` vẫn false.

**Affected files:** `backend/app/services/stream_service.py`.

**Risk:** Nếu URL sai, user vẫn thấy “Stream đã kết nối” nhưng không có hình (worker mở thất bại). Có thể cải thiện sau bằng cách báo lỗi khi worker không mở được (ví dụ qua /stream/status hoặc WebSocket).

---

### 2025-03-14 – [vite] http proxy error: ECONNREFUSED trên /api/v1/*

**Symptom:** Terminal frontend (Vite) báo lặp: `[vite] http proxy error: /api/v1/stats`, `/api/v1/settings`, `/api/v1/models` với `AggregateError [ECONNREFUSED]`.

**Cause:** Backend (FastAPI) chưa chạy — không có process lắng nghe port 8000. Frontend gọi API khi load (getStats, getSettings, list models) nên proxy tới localhost:8000 bị từ chối.

**Fix (vận hành):** Chạy backend trước khi mở frontend: `.\run_backend.bat` hoặc trong `backend`: `python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload`.

---

### 2025-03-14 – Connect vẫn đứng dù đã chạy backend trước (URL YouTube sai)

**Symptom:** Đã chạy backend rồi mới chạy frontend, nhưng bấm Connect vẫn kẹt "Connecting...", No Stream, WS Offline. Ô nhập có link YouTube dạng `.../watch?v=(` (thiếu/sai video ID).

**Cause:** Link YouTube không hợp lệ (video ID thiếu hoặc sai) vẫn được gửi xuống backend. Backend gọi yt-dlp để resolve; với URL lỗi yt-dlp có thể chạy rất lâu (hoặc đến khi timeout 30s), nên POST /stream/start không trả về → frontend treo ở "Connecting...".

**Fix:** (1) **Backend:** Thêm `validate_youtube_url()` trong `video_utils`: kiểm tra URL có video ID đủ dài (10–15 ký tự, [a-zA-Z0-9_-]). Nếu giống YouTube nhưng ID không hợp lệ thì raise ValueError ngay, không gọi yt-dlp. (2) **Frontend:** Trước khi gọi startStream, nếu URL chứa youtube.com/watch hoặc youtu.be thì kiểm tra có đoạn video ID 10–15 ký tự; nếu không thì toast "Link YouTube không hợp lệ" và return, không gửi request.

**Files:** `backend/app/utils/video_utils.py`, `backend/app/services/stream_service.py`, `frontend/src/pages/Dashboard/Dashboard.tsx`.

---

### 2025-03-14 – Test link YouTube G_G8A6JU_LI: resolve fail vì chưa cài yt-dlp

**Test URL:** https://www.youtube.com/watch?v=G_G8A6JU_LI

**Kết quả:** Validation (frontend + backend) đều pass. `resolve_youtube_url()` trả về None vì trong venv backend **chưa cài yt-dlp** (No module named yt_dlp) → backend raise "Không thể lấy stream từ link YouTube...".

**Fix:** Cài yt-dlp trong venv: `cd backend` → `.\venv\Scripts\pip install yt-dlp` (hoặc `pip install -r requirements.txt`). Sau khi cài, resolve trả về URL HLS (manifest.googlevideo.com), Connect có thể chạy. Lưu ý: OpenCV có thể không mở được một số HLS URL tùy build; nếu vậy sẽ thấy "Stream đã kết nối" nhưng không có frame.

---

### 2025-03-14 – POST /stream/start với YouTube treo (HTTP không trả về)

**Symptom:** Gọi POST /stream/start với URL YouTube thì request không trả về, frontend treo "Connecting...", client timeout.

**Cause:** resolve_youtube_url() chạy đồng bộ trong request handler; yt-dlp có thể chạy lâu (Windows/network) → HTTP không trả lời. Ngoài ra start() gọi stop() trước khi spawn thread; stop() gọi _thread.join(timeout=4) → nếu có stream cũ, request bị block tới 4s.

**Fix (non-blocking end-to-end):**
- **Backend:** start(url) không làm gì nặng trên request thread: chỉ clear error, spawn **một** daemon thread chạy _do_start_with_stop(url). Trong thread: gọi stop() (join worker cũ), rồi _do_start(url) (validate YouTube, resolve_youtube_url(), start worker). POST /stream/start trả **202 Accepted** ngay với message "Stream đang kết nối (YouTube có thể mất vài giây)...". Khi resolve/connect lỗi, set _stats.stream_error và _last_error; GET /stream/status trả về { active, fps, frame_count, error }.
- **Frontend:** Sau khi startStream() thành công (202), hiện toast "Stream đang kết nối...", bật streamOn. Poll GET /stream/status mỗi 1.5s tối đa 25s: nếu status.error thì toast lỗi và set streamOn = false; nếu status.active thì dừng poll.
- **Models:** VehicleStats có stream_error: str = ""; stream_routes stream_status trả error từ stream_error.

**Affected files:** `backend/app/services/stream_service.py`, `backend/app/api/stream_routes.py`, `backend/app/models/detection_model.py`, `frontend/src/services/api.ts` (StreamStatus, getStatus), `frontend/src/pages/Dashboard/Dashboard.tsx` (poll sau Connect), `frontend/src/types/detection.ts` (stream_error optional).

**Lessons:** Để HTTP không bao giờ treo: mọi việc tốn thời gian (resolve, open stream, stop + join) phải chạy trong background thread; request handler chỉ spawn thread và return 202. Frontend cần poll status (hoặc WebSocket có error) để hiển thị lỗi khi resolve/connect thất bại.

**Risk:** User phải **restart backend** sau khi pull code mới thì 202 mới có hiệu lực; nếu vẫn chạy process cũ thì request vẫn có thể block.

---

### 2026-03-14 – Stream chạy nhưng không hiện video ở khung giữa

**Symptom:** Backend log cho thấy stream đã start (YouTube/RTSP worker chạy bình thường) nhưng giao diện vẫn hiển thị trạng thái "Kết nối để xem video" ở khung giữa, không có khung hình hoặc bounding box.

**Evidence:** 
- `StreamService` vẫn encode frame + detections và lưu vào `_latest` (`get_latest()`), nhưng không có route nào trả payload này ra API – chỉ có `/api/v1/stream/start` và `/api/v1/stream/status`.
- Frontend hook `useDetection` khởi tạo `currentFrame` và `detections` bằng `useState`, nhưng **không bao giờ gọi `setCurrentFrame`/`setDetections`**. Hook chỉ tải `stats` 1 lần qua `/stats` và quản lý start/stop stream, nên `VideoPlayer` luôn nhận `frame = null` → hiển thị empty-state.

**Root cause:** Luồng truyền frame/detections từ backend sang frontend bị bỏ quên khi refactor bỏ WebSocket; backend vẫn sinh `payload` nhưng không expose endpoint, frontend không poll nên khung video luôn rỗng.

**Fix:** 
- **Backend:** Thêm route `GET /api/v1/stream/frame` trong `stream_routes.py` trả về payload mới nhất (`frame`, `detections`, `stats`); nếu chưa có frame thì trả `{ frame: null, detections: [], stats }` để giữ nguyên shape JSON.
- **Frontend API:** Mở rộng `streamApi` với `getFrame()` (`api.ts`), dùng kiểu `FramePayload`.
- **Frontend hook:** Trong `useDetection`, thêm polling bằng `setInterval` khi `streamActive = true` để gọi `streamApi.getFrame()`, cập nhật `currentFrame`, `detections` và `stats`. Khi stream dừng thì clear interval và reset `currentFrame`/`detections`.

**Affected files:** `backend/app/api/stream_routes.py`, `frontend/src/services/api.ts`, `frontend/src/hooks/useDetection.ts`.

**Lessons:** Khi bỏ WebSocket hoặc đổi giao thức realtime, phải đảm bảo có đường thay thế truyền payload đầy đủ (frame + detections + stats), và cập nhật hook/state ở frontend tương ứng. Nên viết test/harness đơn giản (ví dụ poll /stream/frame) để tránh tình trạng backend chạy nhưng UI luôn ở empty-state.

---

### 2026-03-14 – Nhãn YOLO không khớp với dataset custom (toàn báo car)

**Symptom:** Mô hình YOLO custom được train với YAML:
`names: ['pedestrian', 'bicycle', 'motorbike', 'bus', 'truck', 'container truck', 'car']`, nhưng khi chạy demo web thì bounding box trên UI gần như chỉ hiển thị nhãn `car` (và một số nhãn không đúng với loại phương tiện).

**Evidence:**
- Trong `backend/app/ml/yolo_model.py` có constant `VEHICLE_CLASSES` map theo COCO: `{0:'person',1:'bicycle',2:'car',3:'motorcycle',5:'bus',7:'truck'}`.
- Hàm `predict()` lọc `cls_id` theo `VEHICLE_CLASSES` và gán `class_name = VEHICLE_CLASSES[cls_id]`.
- Với dataset custom:
  - class 0 = pedestrian → map thành `"person"` (tạm ổn),
  - class 1 = bicycle → `"bicycle"` (ổn),
  - class 2 = motorbike nhưng bị map thành `"car"`,
  - class 3 = bus nhưng bị map thành `"motorcycle"`,
  - class 4,5,6 (truck, container truck, car) **bị loại bỏ hẳn** vì không có trong `VEHICLE_CLASSES`.
- Kết quả: nhiều loại phương tiện bị dán nhãn sai hoặc không được vẽ, khiến người dùng thấy gần như toàn `car`.

**Root cause:** `VEHICLE_CLASSES` giả định mô hình là COCO mặc định; với mô hình custom, `cls_id` theo YAML riêng nên mapping cố định này sai hoàn toàn. Code không lấy `model.names` từ ultralytics, cũng không hỗ trợ đủ số lớp trong dataset.

**Fix:**
- Bỏ constant `VEHICLE_CLASSES`.
- Trong `YOLOModel.load()`, sau khi `YOLO(path)`:
  - Đọc `self._model.names` (dict hoặc list) và build `_class_names: dict[int,str]` từ đó để phản ánh đúng YAML train.
- Trong `predict()`:
  - Không filter theo `VEHICLE_CLASSES` nữa; tạo `RawDetection.class_name` bằng `self._class_names.get(cls_id, str(cls_id))`.
- Reset `_class_names` về `{}` khi `unload()`.

**Affected files:** `backend/app/ml/yolo_model.py`.

**Lessons:** Khi dùng YOLO custom với YAML riêng, luôn lấy mapping lớp từ chính mô hình (`model.names`) thay vì hard-code COCO IDs. Nếu muốn đếm/hiển thị một subset (chỉ vehicle chẳng hạn), thì cũng nên filter bằng tên lớp từ YAML (ví dụ chứa 'car', 'truck', 'bus', 'motorbike') chứ không dựa vào chỉ số COCO mặc định.

---

### 2026-03-14 – Hiển thị GPU/CPU trên header

**Yêu cầu:** Người dùng cần biết backend đang dùng GPU hay CPU (không có thông tin trên UI).

**Fix:**
- **Backend:** Thêm `GET /api/v1/stream/device` trong `stream_routes.py`: gọi `torch.cuda.is_available()` và `torch.cuda.get_device_name(0)` (trong try/except), trả về `{ cuda_available: bool, device_name: string | null }`.
- **Frontend:** `streamApi.getDevice()`; Dashboard state `deviceInfo`, fetch khi mount; thêm StatusPill "GPU" (active/xanh) hoặc "CPU" (inactive/xám) trên header, với optional `title` (tooltip) = tên GPU khi có.
- **StatusPill:** Hỗ trợ prop tùy chọn `title?: string` (native tooltip).

**Affected files:** `backend/app/api/stream_routes.py`, `frontend/src/services/api.ts`, `frontend/src/pages/Dashboard/Dashboard.tsx`.

---

### 2026-03-14 – Layout zoom 110%: tên model bị cắt, header dễ tràn

**Symptom:** Ở zoom 110%, tên model trong sidebar hiển thị dạng "yolov26/yolo26m_2..." (truncate mạnh), không có cách xem tên đầy đủ; header có thể bị tràn khi viewport hẹp.

**Expected:** Tên model khi bị cắt vẫn có tooltip hiện full name khi hover; sidebar và header không overflow, truncate hoạt động đúng trong flex.

**Evidence:** ModelUploader dùng `flex-1 truncate` trên span tên model nhưng flex container không có `min-w-0` → truncation trong flex có thể không thu nhỏ đúng. Không có `title` nên user không thấy full name. Header dùng `justify-between` với khối pills cố định bên phải, không wrap → khi zoom 110% (viewport nhỏ hơn) dễ tràn ngang.

**Root cause:** (1) Trong flex, phần tử con muốn truncate cần `min-w-0` (hoặc cha có min-w-0) để flex cho phép thu nhỏ dưới kích thước nội dung. (2) Thiếu tooltip cho tên model. (3) Header chưa cho phép pills xuống dòng khi chật.

**Fix:**
- **ModelUploader:** Thêm `min-w-0` cho row và cho span tên model; thêm `title={m.name}` để hover hiện full name.
- **Dashboard header:** Thêm `min-w-0` cho khối title, `shrink-0` cho logo và FPS; khối pills dùng `flex-wrap justify-end`; header đổi `h-14` thành `min-h-14 py-2` để khi pills wrap vẫn đủ chỗ.

**Affected files:** `frontend/src/components/ModelUploader/ModelUploader.tsx`, `frontend/src/pages/Dashboard/Dashboard.tsx`.

**Lessons:** Flex + truncate cần `min-w-0` trên phần tử bị truncate (hoặc cha trực tiếp). Nên thêm `title` cho mọi chỗ truncate để UX tốt ở mọi zoom. Header nhiều pill nên cho phép wrap thay vì overflow.

---

### 2026-03-14 – Sidebar không scroll, card cảm giác bị đè (zoom 100%)

**Symptom:** Ở zoom 100% người dùng thấy các card trong sidebar (Model, RTSP, ROI, Cài Đặt) dồn sát nhau, cảm giác bị \"đè\" và không thể scroll sidebar; khi zoom 90% thì trông bình thường.

**Evidence:** Root layout dùng `<div className=\"flex flex-col h-screen overflow-hidden\">`; body row là `<div className=\"flex flex-1\">` (lúc đầu thiếu `min-h-0`). Sidebar là flex child rộng cố định, khi nội dung cao hơn viewport thì flex row đẩy chiều cao cha lên nhưng do root overflow hidden nên không có scrollbar, dẫn đến cảm giác chồng lấn. Sau fix: body row dùng `min-h-0`, sidebar có `overflow-y-auto`, main cũng có `min-h-0`.

**Root cause:** Flex container chính không cho phép con co lại theo chiều dọc (`min-height` mặc định), và sidebar thiếu `overflow-y-auto`. Khi nội dung cao hơn viewport (nhất là 100–125% zoom), layout không có vùng scroll nội bộ, khiến card bị dồn và trông như chồng lên nhau.

**Fix:** 
- Thêm `min-h-0` cho body row và main: `<div className=\"flex flex-1 min-h-0\">`, `<main className=\"flex flex-1 min-w-0 min-h-0 overflow-hidden\">`.
- Đặt `overflow-y-auto` cho sidebar: `<aside className=\"w-80 ... overflow-y-auto bg-white flex flex-col gap-2 p-3\">`.
- Đảm bảo `SideCard` và nội dung bên trong không dùng height cố định, chỉ dùng padding/gap.

**Affected files:** `frontend/src/pages/Dashboard/Dashboard.tsx`.

**Lessons:** Với bố cục full-screen dùng flex, luôn dùng `min-h-0` cho các flex child dọc cần chứa vùng scroll, và gán `overflow-y-auto` trực tiếp cho vùng đó (sidebar, stats). Tránh rely vào page scroll khi root đang `overflow-hidden`, đặc biệt vì zoom khác nhau sẽ làm nội dung vượt viewport.

---

### 2026-03-14 – Tất cả bounding box đều hiển thị ID0 (SORT tracker)

**Symptom:** Trong giao diện web mới (FastAPI + React), mọi bounding box đều hiển thị `ID0` (ví dụ `car ID0 58%`, `motorbike ID0 88%`) dù đang bật tracker SORT/DeepSORT.

**Evidence:**
- Backend dùng lớp `SortTrackerAdapter` trong `backend/app/ml/tracker.py`, map output của thư viện SORT thành `Track.track_id`.
- Adapter giả định output có dạng `[x1, y1, x2, y2, id]`. Tuy nhiên một số implementation SORT (6 cột) trả về `[x1, y1, x2, y2, score, id]`.
- Trong code cũ, `track_id` luôn lấy từ `row[4]`. Nếu cột 4 thực ra là confidence (0–1), việc `int(row[4])` sẽ luôn cho `0`, dẫn tới toàn bộ track có `track_id = 0` và UI vẽ `ID0` cho mọi object.

**Root cause:** Giả định sai về sơ đồ cột trong output của SORT – luôn dùng cột 4 làm `track_id` kể cả với những thư viện trả về 6 cột `[x1,y1,x2,y2,score,id]`, khiến score bị cast thành ID=0.

**Fix applied:**
- Trong `SortTrackerAdapter.update()`:
  - Sau khi `out = np.atleast_2d(out)`, đọc `n_cols = out.shape[1]`.
  - Nếu `n_cols >= 6` → lấy `track_id` từ `row[-1]` (cột cuối, tương thích với format `[x1,y1,x2,y2,score,id]` và các format mở rộng).
  - Nếu không (5 cột) → giữ nguyên hành vi cũ: `track_id` từ `row[4]` (đúng với bản SORT gốc của abewley).
  - Hàm `_prune_ids` cũng được cập nhật để lấy ID từ cột cuối khi `n_cols >= 6`.

**Affected files:** `backend/app/ml/tracker.py`.

**Lessons / remaining risks:**
- Khi wrap thư viện bên ngoài (SORT/DeepSORT), luôn kiểm tra kỹ format output (số cột, vị trí ID) và viết adapter để xử lý nhiều biến thể thông dụng (5 hoặc 6 cột) thay vì hard-code một layout duy nhất.
- Nếu gặp implementation SORT khác (ví dụ output MOT với 8+ cột), adapter hiện tại có thể vẫn không khớp – khi đó nên bổ sung nhánh parse riêng hoặc ép dùng đúng package đã kiểm thử (ví dụ sort của abewley hoặc sort-tracker-py với format rõ ràng).

---

### 2026-05-01 – One-click local startup via `run.bat`

**Yêu cầu:** Chạy toàn bộ project chỉ bằng 1 file `run.bat` (không cần mở riêng `run_backend.bat` / `run_frontend.bat`).

**Fix:**
- Nâng cấp `run.bat` thành luồng chạy one-click:
  - Kiểm tra sẵn `python` và `npm` trong PATH, báo lỗi rõ nếu thiếu.
  - Tự tạo `backend\.env` từ `backend\.env.example` nếu chưa có.
  - Tự chọn venv (`venv-gpu` ưu tiên, fallback `venv`), tự tạo venv nếu chưa tồn tại.
  - Tự cài dependencies backend lần đầu và ghi marker `.deps_installed`.
  - Tự cài dependencies frontend nếu thiếu `node_modules`.
  - Khởi động backend trong cửa sổ riêng, chờ backend bind port `8000` (tối đa 60s), sau đó mới chạy frontend để giảm lỗi proxy `ECONNREFUSED`.
  - Tự mở trình duyệt vào `http://localhost:5173`.

**Affected files:** `run.bat`.

**Tradeoffs:**
- Marker `.deps_installed` giúp lần chạy sau nhanh hơn, nhưng khi đổi `requirements.txt` cần xóa marker để ép cài lại.
- Script hiện dùng `Get-NetTCPConnection` để chờ cổng backend; phù hợp Windows PowerShell mặc định.

---

### 2026-05-01 – Giải thích cơ chế Traffic Light panel

**Nội dung đã giải thích:**
- Panel `TrafficLightPanel` poll state/rl/ga mỗi 2 giây và gọi các API `decide`, `advance`, `mode`, `rl/train`, `ga/start`, `simulate`.
- `TrafficLightService.decide()` chọn green time theo mode:
  - `manual`: cố định 30s
  - `fuzzy`: fuzzy Mamdani từ queue + wait
  - `rl`: DQN chọn action (15/25/35/50/65s)
  - `auto`: trộn `0.7 * RL + 0.3 * Fuzzy`
- Green time luôn bị clamp trong [10, 70] giây; `advance_phase()` đảo phase 0/1 và tăng cycle.
- RL dùng reward: `-(avg_wait*0.1 + queue*0.05 + switch*2.0) + throughput*0.5`.
- GA tối ưu 39 gene tham số membership function của fuzzy bằng fitness từ simulator.

**Hiểu nhầm đã làm rõ:**
- Service có hàm `update_from_counts()` để nhận count_in/count_out từ pipeline detection, nhưng hiện chưa thấy nơi nào gọi hàm này trong backend.
- Vì vậy panel traffic light hiện thiên về mô phỏng/logic tối ưu nội bộ hơn là điều khiển theo dữ liệu đếm xe realtime end-to-end.

**Follow-up được gợi ý:**
- Nối `stream_service` -> `traffic_light_service.update_from_counts(...)` ngay trong vòng lặp xử lý frame để đèn phản ứng dữ liệu thật.

---

### 2026-05-09 – Xác minh RTSP transport (TCP hay UDP)

**Yêu cầu:** Kiểm tra backend đang mở RTSP camera bằng TCP hay UDP.

**Kết luận:**
- Các luồng RTSP chính trong backend hiện cấu hình OpenCV/FFmpeg với `OPENCV_FFMPEG_CAPTURE_OPTIONS` chứa `rtsp_transport;tcp`.
- Áp dụng tại các worker stream: `_worker`, `_companion_worker`, `_extra_worker`, và luồng preview `_grab_thumbnail`.
- Do đó mặc định hiện tại hệ thống đang ưu tiên **TCP** cho RTSP.

**Lưu ý kỹ thuật:**
- Ở `_worker`, việc set biến môi trường được guard theo điều kiện URL (không có `?` và không chứa `rtsp_transport`), nhưng những luồng RTSP còn lại vẫn set option TCP.
- Nếu process được chạy với `OPENCV_FFMPEG_CAPTURE_OPTIONS` khác từ môi trường bên ngoài, behavior có thể bị ảnh hưởng.

### 2026-05-09 – Giảm lag RTSP: thumbnail cache + bỏ FFmpeg low-latency flags

**Yêu cầu:** Áp dụng fix để giảm hiện tượng lag/`Connecting...` khi nhiều camera preview song song.

**Đã thay đổi:**
- `backend/app/api/stream_routes.py`:
  - Thêm cache thumbnail theo URL với TTL `6.0s` (`_thumb_cache`, `_thumb_cache_lock`) để giảm tần suất mở lại `VideoCapture`.
  - Tách option FFmpeg cho thumbnail và dùng lock (`_ffmpeg_env_lock`) khi set/restore `OPENCV_FFMPEG_CAPTURE_OPTIONS`, tránh nhiễu option toàn cục giữa các request.
  - Bỏ option `fflags;nobuffer|flags;low_delay` ở thumbnail.
  - Thêm check lỗi `cv2.imencode()` và cleanup `cap.release()` trong `finally`.
- `backend/app/services/stream_service.py`:
  - Ở `_worker`, `_companion_worker`, `_extra_worker`: đổi từ `os.environ.setdefault(...)` sang gán trực tiếp `os.environ[...] = ...` cho `OPENCV_FFMPEG_CAPTURE_OPTIONS`.
  - Loại bỏ `fflags;nobuffer|flags;low_delay` khỏi chuỗi option RTSP; giữ `rtsp_transport;tcp|buffer_size|max_delay|stimeout`.

**Kỳ vọng tác động:**
- Giảm số kết nối RTSP phát sinh từ thumbnail poll.
- Giảm lỗi decode HEVC liên quan buffer quá thấp.
- Giảm rủi ro “ai set env trước thì thắng” do `setdefault`.

### 2026-05-09 – Giảm tranh tài nguyên preview vs stream chính

**Yêu cầu:** Tối ưu khu vực chọn camera để không tranh CPU/network với luồng LIVE chính.

**Đã thay đổi:**
- `frontend/src/components/CameraWall/CameraWall.tsx`:
  - Tăng chu kỳ refresh thumbnail từ `8s` lên `10s`.
  - Thêm stagger ban đầu `550ms` mỗi tile để tránh burst request đồng loạt khi mở modal.
  - Tile đang là LIVE (`activeUrl` + `streamOn`) chuyển sang refresh chậm `30s` thay vì refresh thường xuyên.
  - Gọi thumbnail với `fast=true` để giảm số vòng discard HEVC cho preview.
- `backend/app/api/stream_routes.py`:
  - Giảm thread pool thumbnail từ `max_workers=10` xuống `4`.
  - Đổi key cache từ chỉ `url` sang `(url, width, fast_decode)` để cache đúng theo tham số preview.

**Kỳ vọng tác động:**
- Giảm peak kết nối RTSP và giảm burst decode khi mở camera modal.
- Ưu tiên tài nguyên cho stream chính đang chạy inference/realtime.

### 2026-05-09 – CameraWall chỉ refresh tile visible + pause khi tab ẩn

**Yêu cầu:** Tiếp tục giảm tranh tài nguyên từ modal chọn camera.

**Đã thay đổi:**
- `frontend/src/components/CameraWall/CameraWall.tsx`:
  - Thêm `IntersectionObserver` cho từng tile để chỉ fetch thumbnail khi tile nằm trong viewport (có `rootMargin` để prefetch nhẹ).
  - Tile ngoài viewport không gọi thumbnail, chỉ hẹn timer kiểm tra lại.
  - Thêm lắng nghe `document.visibilitychange`: khi tab ẩn thì tạm dừng fetch; khi tab hiện lại thì resume nhanh các tile visible.

**Kỳ vọng tác động:**
- Giảm thêm số request thumbnail khi danh sách camera dài hoặc cuộn.
- Giảm tải backend/NVR khi user chuyển tab hoặc cửa sổ không active.

### 2026-05-09 – Giải thích luồng JPEG frame/detection

**Câu hỏi:** JPEG hiện có ổn định không, detect và media có đi chung một kênh frame không?

**Kết luận kỹ thuật:**
- Ở mode JPEG (WS `/ws/stream` và `/ws/companion`), backend đóng gói **cùng một message binary** gồm header JSON (`detections`, `stats`) + `jpeg_bytes` bằng `pack_frame_message(...)`.
- Frontend `useWebSocket.parseBinaryFrame(...)` tách lại thành `frame_blob` + metadata và `useDetection.handleWsMessage(...)` cập nhật frame + detections cùng pha.
- Do đó với JPEG: detect và hình đi chung payload/kênh theo từng camera, có fallback HTTP polling (`/api/v1/stream/frame`, `/companion/frame`) khi WS lỗi.
- Ở mode H264 thì video live đi kênh riêng (`/ws/stream-h264/{slot}`), còn detections vẫn theo kênh detect (JPEG/polling), nên không còn “chung một kênh media” như JPEG.

### 2026-05-09 – Fix startup không ưu tiên `best.engine`

**Symptom:**
- Khi khởi động app, hệ thống thường auto-load `best.pt` thay vì `best.engine`.

**Root cause:**
- `backend/app/main.py` chỉ scan fallback theo `.pt` (`**/best.pt`, rồi `**/*.pt`) trước, nên không chọn `.engine` khi không chỉ định rõ.
- `backend/app/ml/yolo_model.py` lưu `model_path/weights_path` theo đường dẫn input ban đầu, không theo artifact thực tế đã load sau bước ưu tiên backend.

**Fix applied:**
- `backend/app/main.py`:
  - Cập nhật thứ tự auto-load: `DEFAULT_MODEL` -> `**/best.engine` -> `**/best.pt` -> `**/*.engine` -> `**/*.pt`.
- `backend/app/ml/yolo_model.py`:
  - Sau `_load_with_backend_preference(...)`, cập nhật `self._model_path` và `self._weights_path` theo `loaded_path` thực tế.
  - Áp dụng tương tự cho `load_pretrained(...)` để metadata runtime phản ánh đúng artifact đang chạy.

**Verification:**
- `python -m py_compile backend/app/main.py backend/app/ml/yolo_model.py` -> pass.
- `ReadLints` các file sửa -> không có lỗi mới.